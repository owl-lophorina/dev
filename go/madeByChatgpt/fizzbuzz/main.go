/*
問題：FizzBuzzを改造せよ

課題内容
1から100までの整数を順番に表示するプログラムを作成しなさい。
ただし、以下の条件を満たすこと：

・3で割り切れる数のときは数字の代わりに "Fizz" と表示する。
・5で割り切れる数のときは数字の代わりに "Buzz" と表示する。
・3と5の両方で割り切れる数のときは "FizzBuzz" と表示する。
・上記以外の場合はそのまま数字を表示する。

追加チャレンジ（時間に余裕があれば）
・出力する上限の数字（例：100）をユーザーから入力できるようにする。
・ループの書き方を「条件付きのfor文」「無限ループ＋break」でそれぞれ書いてみる。//やらない
*/

package main

import (
	"fmt"
)

func main() {
	var num int
	fmt.Println("上限の数値を入力してください")
	fmt.Scan(&num)

	i := 1
	for i <= num {
		if i%3 == 0 {
			if i%5 == 0 {
				fmt.Println("FizzBuzz")
			} else {
				fmt.Println("Fizz")
			}
		} else if i%5 == 0 {
			fmt.Println("Buzz")
		} else {
			fmt.Println(i)
		}
		i++
	}
}

/*
chatGPTの採点

採点
for文の使い方：100点
条件分岐の正確さ：100点
入力処理の導入：90点（エラー処理があると更に良い）
Goの書き方の丁寧さ（idiomaticさ）：90点

合計：95点（ほぼ完璧！）

ちょい改善ポイント
入力のエラー処理と値のバリデーション（1以上）を入れると堅牢。
ループは Go では↓がより慣用的です：for i := 1; i <= num; i++ { ... }
条件は今のままでもOKですが、i%15 == 0 を先に見るとさらにスッキリ。
*/
